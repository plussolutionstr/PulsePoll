<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:PulsePoll.Mobile.ViewModels"
             xmlns:behaviors="clr-namespace:PulsePoll.Mobile.Behaviors"
             xmlns:converters="clr-namespace:PulsePoll.Mobile.Converters"
             x:Class="PulsePoll.Mobile.Views.WalletWithdrawPage"
             x:DataType="viewModels:WalletWithdrawViewModel"
             Title="Para Çek">

    <ContentPage.Resources>
        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="20,16" Spacing="16">

            <!-- Mini Balance Card -->
            <Border StrokeThickness="0"
                    StrokeShape="RoundRectangle 20"
                    BackgroundColor="{AppThemeBinding Light={StaticResource PrimaryLight}, Dark={StaticResource PrimaryLightDark}}"
                    Padding="20,16">
                <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                    <Border StrokeThickness="0"
                            StrokeShape="RoundRectangle 20"
                            HeightRequest="40" WidthRequest="40"
                            BackgroundColor="{AppThemeBinding Light={StaticResource PrimaryMid}, Dark={StaticResource PrimaryMidDark}}">
                        <Label Text="&#x20BA;"
                               FontFamily="JakartaExtraBold" FontSize="20"
                               TextColor="{AppThemeBinding Light={StaticResource PrimaryDark}, Dark={StaticResource PrimaryDarkDark}}"
                               HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Border>
                    <VerticalStackLayout Spacing="2" VerticalOptions="Center">
                        <Label Text="Çekilebilir Bakiye"
                               FontFamily="JakartaRegular" FontSize="12"
                               TextColor="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource TextSecondaryDark}}"/>
                        <Label Text="{Binding WithdrawableBalanceInfo}"
                               FontFamily="JakartaBold" FontSize="16"
                               TextColor="{AppThemeBinding Light={StaticResource PrimaryDark}, Dark={StaticResource PrimaryDarkDark}}"/>
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Border>

            <!-- Bank Account Selection -->
            <Border Style="{StaticResource CardBorderStyle}" Padding="16">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Banka Hesabı"
                           FontFamily="JakartaSemiBold"
                           FontSize="13"
                           TextColor="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource TextSecondaryDark}}" />
                    <Border StrokeShape="RoundRectangle 12"
                            Stroke="{AppThemeBinding Light={StaticResource CardBorder}, Dark={StaticResource CardBorderDark}}"
                            StrokeThickness="1"
                            BackgroundColor="{AppThemeBinding Light={StaticResource PageBackground}, Dark={StaticResource PageBackgroundDark}}"
                            Padding="12,0">
                        <Picker Title="Hesap seçin"
                                ItemsSource="{Binding BankAccounts}"
                                ItemDisplayBinding="{Binding DisplayTitle}"
                                SelectedItem="{Binding SelectedBankAccount}"
                                FontFamily="JakartaSemiBold"
                                FontSize="15"
                                HeightRequest="48"
                                VerticalOptions="Center" />
                    </Border>
                </VerticalStackLayout>
            </Border>

            <!-- Amount Entry -->
            <Border Style="{StaticResource CardBorderStyle}" Padding="16">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Tutar"
                           FontFamily="JakartaSemiBold"
                           FontSize="13"
                           TextColor="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource TextSecondaryDark}}" />

                    <Border StrokeShape="RoundRectangle 12"
                            Stroke="{AppThemeBinding Light={StaticResource CardBorder}, Dark={StaticResource CardBorderDark}}"
                            StrokeThickness="1"
                            BackgroundColor="{AppThemeBinding Light={StaticResource PageBackground}, Dark={StaticResource PageBackgroundDark}}"
                            Padding="0">
                        <Grid ColumnDefinitions="48,*,48">
                            <Label Text="&#x20BA;"
                                   FontFamily="JakartaBold" FontSize="18"
                                   TextColor="{AppThemeBinding Light={StaticResource PrimaryPurple}, Dark={StaticResource PrimaryPurpleDark}}"
                                   HorizontalOptions="Center" VerticalOptions="Center"/>
                            <Entry Grid.Column="1"
                                   Placeholder="0,00"
                                   Text="{Binding Amount}"
                                   MaxLength="16"
                                   Keyboard="Numeric"
                                   FontFamily="JakartaSemiBold"
                                   FontSize="18">
                                <Entry.Behaviors>
                                    <behaviors:DecimalEntryBehavior />
                                </Entry.Behaviors>
                            </Entry>
                            <Label Grid.Column="2"
                                   Text="TL"
                                   FontFamily="JakartaSemiBold" FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource TextTertiaryDark}}"
                                   HorizontalOptions="Center" VerticalOptions="Center"/>
                        </Grid>
                    </Border>

                    <!-- Info Hint -->
                    <Border StrokeThickness="0"
                            StrokeShape="RoundRectangle 10"
                            BackgroundColor="{AppThemeBinding Light={StaticResource PrimaryLight}, Dark={StaticResource PrimaryLightDark}}"
                            Padding="12,10">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding ConversionInfo}"
                                   FontFamily="JakartaRegular"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light={StaticResource PrimaryDark}, Dark={StaticResource PrimaryDarkDark}}" />
                        </VerticalStackLayout>
                    </Border>
                </VerticalStackLayout>
            </Border>

            <!-- Buttons -->
            <Grid HeightRequest="52">
                <Button Text="Talep Gönder"
                        Style="{StaticResource PrimaryButtonStyle}"
                        HorizontalOptions="Fill"
                        IsVisible="{Binding IsSaving, Converter={StaticResource InvertedBoolConverter}}"
                        Clicked="OnSubmitClicked" />
                <Border StrokeThickness="0"
                        StrokeShape="RoundRectangle 26"
                        BackgroundColor="{AppThemeBinding Light={StaticResource PrimaryPurple}, Dark={StaticResource PrimaryPurpleDark}}"
                        HeightRequest="52"
                        IsVisible="{Binding IsSaving}">
                    <ActivityIndicator IsRunning="True"
                                       Color="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource WhiteDark}}"
                                       HeightRequest="24" WidthRequest="24"
                                       HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
            </Grid>

            <Button Text="İptal"
                    Style="{StaticResource SecondaryButtonStyle}"
                    HeightRequest="52"
                    CornerRadius="26"
                    HorizontalOptions="Fill"
                    IsEnabled="{Binding IsSaving, Converter={StaticResource InvertedBoolConverter}}"
                    Clicked="OnCancelClicked" />

            <BoxView HeightRequest="20" Color="Transparent"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
